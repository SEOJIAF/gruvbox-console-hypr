#!/usr/bin/env bash
set -euo pipefail

# Installs the gruvbox-console-hypr rice on top of an existing Fedora 44 +
# KDE Plasma system. Never touches Plasma packages, config, or the display
# manager's enabled/active state. Idempotent — safe to re-run.
#
# TODO(phase 1+): package installs, COPR enables, symlinking, session entry.

echo "install.sh: placeholder — populated during later phases."
